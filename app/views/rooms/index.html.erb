<div class="container text-center general-info">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
      <h1>Cabinas</h1>

      <% @rooms.each do |r| %>
      <%= link_to r.name, room_schedules_path(r), class: "btn-sm btn-primary" %>
      <br>
      <% end %>
    </div>
  </div>
</div>
